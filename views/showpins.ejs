<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=11">
    <title>Show Pins</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class="h-screen">
    <h1 class="text-2xl text-white font-bold text-center py-12 bg-[#ff0000]"><%= user.posts[0].description %></h1>
    <div class="w-[80%] min-h-screen m-auto my-12 columns-4 gap-5">
        <% user.posts.forEach(function(post) { %>
        <div class="rounded-lg overflow-hidden mb-5">
            <div class="w-full h-full bg-black">
                <img src="/images/uploads/<%= post.image %>" class=" w-full h-full object-cover" alt="">
            </div>
        </div>
        <% }) %>
    </div>
</body>
</html>